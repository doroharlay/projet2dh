{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Command</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Totalprice</th>
                <td>{{ entity.totalPrice }}</td>
            </tr>
            <tr>
                <th>Isvalidate</th>
                <td>{{ entity.isValidate }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('command') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('command_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
